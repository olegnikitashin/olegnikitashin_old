<%= simple_form_for(@portfolio_item, html: { class: 'form-horizontal' }) do |f| %>
    <%= f.input :title %>
    <%= f.input :subtitle %>
    <%= f.input :body %>
    <%= f.button :submit %>
<% end %>